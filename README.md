# FastAPI_MongoDB
Fast api todo app with mongodb

# create virtual env... e.g.- venv
# install fastapi in venv == pip install fastapi uvicorn pymongo
# inside configurations. py > uri='' put your mongodb string
# for run the code===> uvicorn main:app
